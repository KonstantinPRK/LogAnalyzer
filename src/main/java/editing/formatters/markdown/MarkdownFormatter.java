package editing.formatters.markdown;

import collection.outputting.reporter.LogReport;
import editing.formatters.Formatter;

public class MarkdownFormatter implements Formatter {
    @Override
    public String format(LogReport report) {
        return "";
    }
}
