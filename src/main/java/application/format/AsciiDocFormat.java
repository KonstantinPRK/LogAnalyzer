package application.format;

import application.model.LogReport;

public class AsciiDocFormat implements Format {
    @Override
    public String format(LogReport report) {
        return "";
    }
}
