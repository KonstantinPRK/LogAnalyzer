package application.userInterface.console.format;

import application.components.reporting.logReport.Report;

public interface Format {
    String format(Report report); // пока не знаю что получает ведь не знаюч то важно
}
